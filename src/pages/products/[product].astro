---
import Layout from '../../layouts/Layout.astro';
import ProductDetails from '../../components/product/productDetail.jsx'

const fullPath = Astro.url.pathname;
console.log("imprimiendo el path desde [product].astro: ", fullPath);

---

<Layout 
    title="Detalles del Producto",
    description='Descripcion de producto'
    >
    <ProductDetails client:load fullPath={fullPath} />
</Layout>